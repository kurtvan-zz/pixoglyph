let mocha = require('mocha');
let chair = require('chai');
let pixoglyph = require('../src/pixoglyph');

let assert = chai.assert;

describe('Colors', () => {
    describe('setCharColor()', () => {
        it('should set the correct character\'s color', () => {
            let p = new Pixoglyph(20, 10, '.');
            
        })
    })
})
